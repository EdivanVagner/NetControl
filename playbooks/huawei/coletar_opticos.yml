  name: 🔍 Coletar Informações do Transceiver (Óptica) em Switches Huawei
  hosts: huawei_switch
  gather_facts: no
  connection: network_cli
  
  # Variáveis de Conexão (Ajuste conforme seu ambiente)
  vars:
    ansible_network_os: community.network.cnos # Usado para o tipo de conexão VRP
    ansible_user: "AlphaSoares"
    ansible_password: "Soares99RS@"
   ## ansible_become: yes
   ## ansible_become_method: enable # Para entrar em modo privilegiado, se necessário
   ## ansible_become_pass: "sua_senha_enable"
    
  tasks:
   name: Executar 'display interface transceiver verbose'
   ansible.netcommon.cli_command:
        # Este comando exibe todos os transceivers e suas informações detalhadas,
        # incluindo potência de transmissão (Tx) e recepção (Rx).
        command: display interface transceiver verbose
   register: optical_output # Registra a saída do comando nesta variável
      
   name: Exibir a saída da óptica no console (Opcional)
   ansible.builtin.debug:
        msg: "Saída da óptica para {{ inventory_hostname }}:\n{{ optical_output.stdout[0] }}"
