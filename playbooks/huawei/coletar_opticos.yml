---
- name: 🔍 Coletar Informações do Transceiver (Óptica) em Switches Huawei
  hosts: 10.0.92.0
  gather_facts: no
  connection: network_cli
  
  # Variáveis de Conexão (Ajuste conforme seu ambiente)
  vars:
    ansible_network_os: community.network.cnos # Usado para o tipo de conexão VRP
    #ansible_user: "User"
    #ansible_password: "Pass"
    # Se precisar de modo privilegiado (enable) para executar o comando:
    # ansible_become: yes
    # ansible_become_method: enable
    # ansible_become_pass: "sua_senha_enable"
    
  tasks:
    # ------------------ PRIMEIRA TASK --------------------
    # O traço (-) define uma nova task na lista
    - name: 📊 Executar 'display interface transceiver verbose'
      ansible.netcommon.cli_command:
        # 'command' é um argumento do módulo, indentado corretamente
        command: display interface transceiver
      # 'register' é uma chave da task, alinhada com o módulo e o 'name'
      register: optical_output 
      
    # ------------------ SEGUNDA TASK --------------------
    # Um novo traço (-) para a próxima task
    - name: 📋 Exibir a saída da óptica no console (Opcional)
      ansible.builtin.debug:
        msg: "Saída da óptica para {{ inventory_hostname }}:\n{{ optical_output.stdout[0] }}"
